def solution(brown, yellow):
    
    total = brown + yellow
    
    # 약수 다 구한다
    for b in range(1, total+1):
        if (total / b) % 1 == 0:
            a = total / b
            if a >= b:
                if 2*a + 2*b == brown + 4:
                    return [a, b]

## 약수를 굳이 다 새로운 리스트에 저장할 필요가 없다.
from itertools import combinations

def solution(numbers):
    answer = set()
    
    for i in list(combinations(numbers, 2)):
        answer.add(sum(i))
    return sorted(answer)
        
    
    

## 파이썬에는 combinations 이라는 함수가 있다.
## combinations 함수는 리스트에서 원하는 갯수만큼의 요소의 조합을 출력한다!!.
import math

def solution(n, k):
    
    lst = [i for i in range(1, n+1)]
    answer = []
    
    
    while n != 0:
        temp = math.factorial(n-1)
        
        answer.append(lst.pop((k-1) // temp))
        n -= 1
        k = k % temp
        
        
    return answer
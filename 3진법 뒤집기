

def solution(n):
    
    answer =''
    
    while n > 0:
        n, re = divmod(n,3)
        answer += str(re)
    return int(answer, 3)


# divmod 함수를 사용하면 몫과 나머지를 구할수 있다.
# int(x, base): base진법으로 구성된 str 형식의 수를 10진법으로 변환.
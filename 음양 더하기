def solution(absolutes, signs):
    
    answer = 0
    for i in range(len(absolutes)):
        if signs[i] == True:
            answer += absolutes[i]
        else:
            answer -= absolutes[i]
    return answer


; signs 리스트 안의 값이 true 혹은 false라고만 써있으면, 
; string이 아니라 boolean임!